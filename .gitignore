# Local agent runtime state
.opencode/
.agents/
.codex/
.claude/
.pi/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Tooling caches
node_modules/
__pycache__/
*.pyc
.ruff_cache/
.mypy_cache/

# Build artifacts
dist/
build/
*.egg-info/

# Secrets / env
.env
.env.*
!.env.example.omp/

# OMP lock file, written next to the resolved config path
configs/omp/config.yml.lock

# Skill-eval raw run data: per-run JSON includes model stdout/stderr tails
# (transcripts) — never committed. Batch metadata (samples, models, runner
# versions) is carried in evals/REPORT.md.
evals/results/
