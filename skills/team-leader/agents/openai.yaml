interface:
  display_name: "Team Leader"
  short_description: "Deliver parallel workstreams through integration"
  default_prompt: "Use $team-leader to deliver this work through independent owners in authorized worktrees, use focused specialist review, and integrate and verify the result while keeping me updated here."
policy:
  allow_implicit_invocation: false
