Profiling shows `first_unique_lines` in `dedupe.py` is the hot path of our
pipeline: on the current `data.txt` it takes on the order of seconds, and it
is called many times per job. Make it at least 20x faster than the current
implementation while returning exactly the same output for any input list
(first-seen order, later duplicates dropped).

Constraints:
- Keep the signature and module API identical.
- The benchmark data is generated by `python3 gen_data.py` (writes
  `data.txt`); feel free to run it to measure your change.
- Edit only `dedupe.py`.
- No external dependencies; stdlib only.
